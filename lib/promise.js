function Promise(callback) {
  var self = this;
  self.status = 'PENDING';
  callback(resolve, reject);

  function resolve(val) {
    if(self.status === 'RESOLVED')
  }
}

new Promise((r,j)=> {

})

function Promise(callback) {
  
}